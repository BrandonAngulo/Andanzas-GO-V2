const { CULTURAL_SITES } = require('./data/sites.ts');
console.log("Local sites loaded:", CULTURAL_SITES.length);
// Since it's typescript, we might need a simpler regex parser or just run a standard JS translation.
